# Install after the main requirements.txt:
#   pip install -r requirements.txt
#   pip install -r eval/requirements-eval.txt
#
# ragas is pinned to 0.3.9, not latest. ragas 0.4.x has an unconditional
# top-level import of ChatVertexAI from a langchain_community path that
# langchain-community removed - breaks on import even though this project
# never touches Vertex AI. Confirmed against a live GitHub issue while
# building this (vibrantlabsai/ragas#2745), which names 0.3.9 as the last
# version before the break. _ragas_compat.py adds a second layer of
# defense (a sys.modules stub) in case a future transitive bump
# reintroduces the same problem.
ragas==0.3.9
